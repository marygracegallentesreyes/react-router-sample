import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Temporarily remove React.StrictMode for troubleshooting
ReactDOM.render(
  <App />,
  document.getElementById('root')
);
